# `bracketSpacing`

Control whether whitespace should be inserted between brackets or not.

Default option is `false`.

## Example for `false`

```yaml
[a, b]
```

## Example for `true`

```yaml
[ a, b ]
```
